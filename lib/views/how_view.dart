import 'package:flutter/material.dart';

class HowView extends StatelessWidget {
  const HowView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('How')),);
  }
}
